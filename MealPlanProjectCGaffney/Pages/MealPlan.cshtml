@page
@model MealApp.Pages.MealPlanModel
@{
    ViewData["Title"] = "Your Meal Plan";
}

<h1>Your 7-Day Meal Plan</h1>

@if (Model.MealPlan != null)
{
    <pre class="result-box">@Model.MealPlan</pre>
}

<form method="get" asp-page="/Index">
    <button type="submit" class="form-button">← Back to Edit Survey</button>
</form>
